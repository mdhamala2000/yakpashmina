yarn add @airwallex/airTracker

# How to test airTracker
1. under AirTracker Dir, run `yarn link`
2. under AirTracker Dir, run `yarn build`
3. under your project Dir which use AirTracker as an dependency, run `yarn link @airwallex/airtracker`